const r=require('express').Router();const c=require('../controllers/postController');const {auth}=require('../middleware/auth');r.get('/',c.list);r.get('/mine',auth,c.mine);r.get('/:slug',c.getBySlug);r.post('/',auth,c.create);r.put('/:id',auth,c.update);r.delete('/:id',auth,c.remove);module.exports=r;