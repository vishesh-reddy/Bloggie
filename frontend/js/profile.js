const {api,escapeHtml}=Bloggie;async function load(){const id=new URLSearchParams(location.search).get('id');const d=await api('/users/'+id);document.querySelector('#profile').innerHTML=`<div class="profile-head"><img src="${d.user.avatar_url||'/assets/placeholder.svg'}"><div><p class="eyebrow">Author</p><h1>${escapeHtml(d.user.name)}</h1><p>${escapeHtml(d.user.bio||'')}</p></div></div>`;document.querySelector('#profilePosts').innerHTML=d.posts.map(p=>`<a class="post-card" href="/post.html?slug=${encodeURIComponent(p.slug)}"><div class="post-body"><div class="post-meta">${new Date(p.published_at).toLocaleDateString()}</div><h3>${escapeHtml(p.title)}</h3><p>${escapeHtml(p.excerpt||'')}</p></div></a>`).join('')}load();